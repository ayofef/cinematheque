import React from "react";
import Loader from "react-loader-spinner";

const ButtonLoader = () => {
    return (
        <>
            <Loader type="ThreeDots" color="#fff" height={19.1} width={25} />
        </>
    );
};

export default ButtonLoader;
